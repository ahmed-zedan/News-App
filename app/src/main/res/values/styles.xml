<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/nexa_text_light</item>
        <item name="textColorItem">@color/TextColor</item>
        <item name="toolbarTextSize">@dimen/toolbar_title_size</item>
        <item name="colorBackground">@color/colorBackground</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="defaultPadding">@dimen/size_default_padding</item>
        <item name="android:textColor">?attr/textColorItem</item>
        <item name="textSizeItem">@dimen/text_size</item>
        <item name="smallPadding">@dimen/small_padding</item>
        <item name="headerTextSize">@dimen/head_text_size</item>
    </style>
    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">#767676</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
    <style name="ToolbarTextAppearance" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="android:textSize">?attr/toolbarTextSize</item>
    </style>

    <!-- set the rounded drawable as background to your bottom sheet -->
    <style name="BottomSheet" parent="@style/Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_bottom_sheet</item>
    </style>

    <style name="BaseBottomSheetDialog" parent="@style/Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="bottomSheetStyle">@style/BottomSheet</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="BaseBottomSheetDialog" >
        
    </style>

    <style name="RoundRedButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:background">@drawable/bg_round_button</item>
        <item name="android:paddingStart">@dimen/padding_round_button</item>
        <item name="android:paddingEnd">@dimen/padding_round_button</item>
        <item name="android:textColor">?attr/colorBackground</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_marginTop">?attr/defaultPadding</item>
        <item name="android:fontFamily">@font/nexa_text_bold</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="RadioButtonStyle">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:paddingStart">?attr/defaultPadding</item>
        <item name="android:paddingEnd">?attr/defaultPadding</item>
        <item name="android:textAlignment">viewStart</item>
    </style>
    
    <style name="DividerLine">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@android:color/darker_gray</item>
    </style>

</resources>
